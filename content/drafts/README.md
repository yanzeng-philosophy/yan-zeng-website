# Drafts

This directory is for AI-generated or pending-review writing. Draft files are not rendered by the site.

Do not treat this folder as private if the GitHub repository is public. The `.gitignore` file is configured to avoid committing real draft Markdown files by default, while keeping one harmless sample draft.
