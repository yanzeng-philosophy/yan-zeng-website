---
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import ContentList from "../components/ContentList.astro";

const entries = (await getCollection("public")).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);
const latestEntries = entries
  .filter((entry) => !entry.id.startsWith("zh/") && !entry.id.startsWith("ja/"))
  .slice(0, 3);
const siteBase = "https://yanzeng-philosophy.github.io/yan-zeng-website/";
---

<BaseLayout
  title="Yan Zeng"
  description="Yan Zeng's personal academic website for philosophy, AI ethics, trust, and public research notes."
  pageKey="home"
>
  <div class="page">
    <section class="hero" aria-labelledby="home-title">
      <div>
        <p class="eyebrow">PhD Student in Philosophy, University of California, Irvine</p>
        <h1 id="home-title">Yan Zeng</h1>
        <p class="hero__dek">
          I study how AI systems reshape trust, responsibility, and human agency.
          This site is both an academic profile and a public notebook for research,
          writing, projects, and selected reflections.
        </p>
        <div class="hero__actions">
          <a class="button" href={`${siteBase}research/`}>Read research notes</a>
          <a class="button button--secondary" href={`${siteBase}now/`}>See now page</a>
        </div>
      </div>

      <aside class="hero-preview" aria-label="Featured writing">
        <div class="hero-preview__chrome" aria-hidden="true">
          <span class="hero-preview__dot"></span>
          <span class="hero-preview__dot"></span>
          <span class="hero-preview__dot"></span>
        </div>
        <a class="hero-preview__article" href={`${siteBase}notes/essays/soft-enclosure-convenience-dependence/`}>
          <p class="eyebrow">Latest Essay</p>
          <h2>The Soft Enclosure</h2>
          <p>
            When convenience becomes dependence, formal freedom can remain intact
            while the capacities that make freedom usable quietly erode.
          </p>
          <div class="hero-preview__meta">
            <span>Essay</span>
            <span>AI</span>
            <span>Language</span>
          </div>
        </a>
        <div class="hero-preview__tiles" aria-label="Site themes">
          <div class="hero-preview__tile"><strong>Research</strong><span>Trust, responsibility, and AI-mediated practices.</span></div>
          <div class="hero-preview__tile"><strong>Notebook</strong><span>Reviewed essays, logs, projects, and reflections.</span></div>
          <div class="hero-preview__tile"><strong>Now</strong><span>Current work and public-facing priorities.</span></div>
        </div>
      </aside>
    </section>

    <section class="section" aria-labelledby="areas-title">
      <h2 id="areas-title">Site Areas</h2>
      <div class="section-grid">
        <article class="panel">
          <h3>Research</h3>
          <p>Academic work on trust, dependence, responsibility, AI ethics, and social epistemology.</p>
        </article>
        <article class="panel">
          <h3>Notebook</h3>
          <p>Reviewed public notes, essays, projects, and reflections that can become durable intellectual assets.</p>
        </article>
        <article class="panel">
          <h3>Now</h3>
          <p>A short snapshot of current research, writing, and public-facing priorities.</p>
        </article>
      </div>
    </section>

    <section class="section" aria-labelledby="latest-title">
      <h2 id="latest-title">Latest Public Notes</h2>
      <ContentList entries={latestEntries} />
    </section>
  </div>
</BaseLayout>
